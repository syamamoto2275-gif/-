# ペライチ 修正版マクロ 手動インポート手順（2026-07-02）

社長がお一人で最初から最後まで完了できる手順です。上から順にそのまま進めてください。
所要時間：約5分。**この作業でボタンやレイアウトは壊れません**（マクロの中身だけを新しくします）。

## 今回の修正内容（最終版）
1. **各シートの1行目（見出し）の色を消す** … ← 五郎がすでに本番ファイルに反映済み。手動作業は不要。
2. **内容品・品名を「shop fuu / ペット用品」に統一**（クリックも佐川も。ペライチは衣類販売なし）。
3. **佐川データをE飛伝の正規フォーマット(74列)でCSV自動保存**（お手本＝`ペライチ佐川2026.6.29.csv`）。
   - 実行2で佐川が1件以上あると自動保存。ご依頼主(shop fuu/小牧)・荷姿・出荷個数・スピード・クール便・指定シール等を正規どおり埋める。
   - 保存先：`Dropbox\…\【出荷】ペライチ\e飛伝データ ペライチ\`
   - ファイル名：**`ペライチ佐川YYYY.M.D.csv`**（社長の運用どおり／Shift-JIS）
4. **納品書のTEL・Mail（B16/B17/G16）が途中で切れる不具合を修正**（右隣セルの空文字をやめ、はみ出し表示できるように）。

> ※#1（色消し）は反映済み。手動作業が要るのは #2 #3 #4（＝マクロの中身の入れ替え）です。
> インポート後、必ず**実行1→実行2をもう一度**通してください（新しいクリア処理でTEL/Mailの切れも直ります）。

---

## 事前準備

1. **最新の修正版ファイルを取得する**
   五郎（自宅PC）が修正版 `ペライチ_Module_マスター_sjis.bas` をGitHubへpush済みです。
   - 自宅PCの場所：
     `C:\Users\山本　史朗\OneDrive\Desktop\新しいフォルダー\-\ペライチ_Module_マスター_sjis.bas`
   - 会社PCで作業する場合は、先に五郎に「git pullして」と言ってください（最新に更新されます）。

2. **対象のExcelファイルを開く**
   `Dropbox\ネットショップ\【出荷】ペライチ\マクロフォーマット\ペライチ出荷データ作成用フォーマット2026.6.18.xlsm`
   - 上部に黄色い「セキュリティの警告（マクロが無効…）」が出たら、**［コンテンツの有効化］**を押す。

---

## 手順A：古いマクロを外す

1. Excelを開いたまま、キーボードの **Alt キーを押しながら F11** を押す。
   → 「Microsoft Visual Basic for Applications」という別ウィンドウが開きます。
2. 左側の「プロジェクト」欄で、**標準モジュール（Modules）** の左の [＋] をクリックして開く。
3. その中の **「ペライチ_マスター」** を見つけて **右クリック** する。
4. メニューの **「ペライチ_マスターの解放(R)…」** をクリック。
   - 「削除する前に…エクスポートしますか？」と聞かれたら、**［いいえ(N)］** を押す。
   → これで古いマクロが消えます。

---

## 手順B：修正版マクロを読み込む

1. 上のメニュー **「ファイル(F)」→「ファイルのインポート(I)…」** をクリック。
2. ファイル選択画面で、修正版の **`ペライチ_Module_マスター_sjis.bas`** を選び、**［開く］**。
   - 場所：`…\新しいフォルダー\-\ペライチ_Module_マスター_sjis.bas`
3. 左の標準モジュールに **「ペライチ_マスター」** が新しく入ればOK。
4. VBA画面を閉じる（右上の×、またはExcelに戻る）。

---

## 手順C：保存する

1. Excelに戻り、**Ctrl キーを押しながら S**（上書き保存）。
2. 「次の機能は…保存できません」等が出た場合は、**xlsm形式のまま**保存されるので、そのまま［はい］。

---

## 手順D：テストする（1件でOK）

1. **元データ**シートに、ペライチのCSVから注文データを貼り付ける。
   - ※佐川で送る行が最低1件あると、#3のCSV出力まで確認できます。
2. **実行1** ボタンを押す。→ 編集シートで発送方法（A列）を確認。佐川にしたい行は「ペット佐川」「衣類佐川」に。
3. **実行2** ボタンを押す。→ 自動で「ペットクリック物出し」シートが表示されます。

### 直っているか確認するポイント
- [ ] **各シートの1行目に色が付いていない**（#1）
- [ ] クリック一括発行アップデータの **内容品（8列目）が「shop fuu / ペット用品」**（#2）
- [ ] 実行2の完了メッセージに **「佐川E飛伝CSVを保存しました：…」** と保存先パスが表示される（#3）
- [ ] `Dropbox\…\e飛伝データ ペライチ\` に **`ペライチ佐川（今日の日付）.csv`** が出来ている（#3）
- [ ] そのCSVを **佐川e飛伝で取り込めた**（列位置が合っている。お手本6.29と同じ74列）（#3）
- [ ] 納品書の **TEL・Mail（B16/B17/G16）が最後まで表示**されている（切れていない）（#4）

---

## 手順E：確認できたら五郎へ

- うまくいったら「**ペライチOK**」と五郎に伝えてください。
  → 五郎が、テストで入ったお客様データを消して**きれいなマスターひな型**に戻し、成果物とGitHubへ最新版を反映します。
- おかしい点があれば、その画面のまま五郎に症状を教えてください。すぐ直します。

---

*万一戻したくなったら、編集前の原本が
`マクロフォーマット\_backup\ペライチ出荷データ作成用フォーマット2026.6.18_before_（日時）.xlsm` にあります。五郎にお申し付けください。*
