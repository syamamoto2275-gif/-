# 五郎 会話ログ 2026-07-01（自宅PC）

作業PC：**自宅PC**（`C:\Users\山本　史朗\OneDrive\Desktop\新しいフォルダー\-`）

## 今日やったこと

### 1. 2台の五郎＋GitHubを同期
- 自宅PCのリポジトリを fast-forward pull で `1fd44b2` に統一（会社PC・GitHub・自宅PCが完全一致）。
- 破壊的な `reset --hard` は使わず、ロスなしで追従。
- 気づき：起動フォルダ `新しいフォルダー` はGit管理外。本物のリポジトリは1階層下の `-`（ハイフン1文字）フォルダ。`.git`は隠し属性なので探索は `-Force` 必須。

### 2. 言葉遣いルールを明確化（共通ルール.md）
- 社長・目上・お客様に **「了解」禁止 → 「承知しました」**。**全社ルール（人間も新スタッフも同じ）**。
- ただし **五郎同士・秘書同士のカジュアルな会話では「了解」OK**（社長が見ても可）。線引きは「誰に向けた言葉か」。
- 記憶はリポジトリにも書く、をルール4に明記（記憶だけだと片方の五郎に共有されない）。

### 3. 顧客データの取り扱い（共通ルール ルール5 追加）
- テンプレ・マスターに実データを残さない。テスト後は消してから保存・push。

### 4. ペライチ 出荷データ作成フォーマットの大改修（メイン作業）
- Dropboxのテストファイル（納品書統合版）を特定し、**マスターひな型化**（顧客データ除去、マクロ・ボタン保持）。
- 社長テストの指摘に対応：**B/G集約・TEL：/Mail：接頭辞・佐川見出しを行1・I37にお支払い方法・H22単価リセット・実行1後に編集シート表示・実行2後にペットクリック表示・お届け先G17メール削除**。
- **住所の日付化バグを発見・修正**：住所2が「443」等の数字だけだとExcelが日付(1901/3/18)に変換していた。文字コード化けでも列ズレでもなかった。→ 書込先セルを **文字(@)書式** にして解決（マクロ＋実物ファイル両方）。社長テストでOK確認。
- 学びを **出荷マクロ作成スキル** に追記（全チャネル共通で対策）。

## 技術的な判断・確立したこと
- **実物xlsmの安全な編集**：ボタン付きファイルは openpyxl だと壊れる → **Excel COM自動操作**（AutomationSecurity=3でマクロ無効化して開く）で編集。openpyxlは読み取り・検証専用。
- **VBAの自動注入は不可**：この環境はAccessVBOMを有効化してもプログラムからのVBAプロジェクト操作を許可しない（VBComponentsが0件になる）。→ 手動インポート、またはセル書式のCOM修正で対応。
- 顧客データの検証は値を転写せず True/False や書式だけ出す（PII配慮・分類器対策）。

## 社長からの指示・方針
- 「了解」は全社で禁止（人間もAIも新スタッフも）。ただし五郎同士はOK。
- テストファイルをマスターにしたい／元データも消してひな型に。
- 最新情報で共有できるよう同期して。

## 次にやること・未完了
- 特になし（ペライチ完了）。会社PCの五郎は pull で最新マクロ・スキル・ひな型を受け取れる。
- 楽天・Amazon・Wowmaのマクロも、いずれ「数字住所の日付化」対策（NumberFormat=@）を入れると安全（スキルに記載済み）。

## 主なコミット
- 言葉遣い・顧客データ・記憶共有ルール、ペライチ修正一式、住所日付化修正、ひな型・スキル更新まで push 済み。
